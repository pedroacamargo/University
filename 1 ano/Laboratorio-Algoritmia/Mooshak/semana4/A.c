#include <stdio.h>

int main() {
   
  int N;
  char phrase[1001];

  if (scanf("%d", &N) != 1) return 1;

  int n = 0;
  // linhas
  for (int i = 0; i < N; i++) {
    while (scanf("%s", phrase) != 1) {
      
    }



  }

  printf("%d", n);
  return 0;
}
