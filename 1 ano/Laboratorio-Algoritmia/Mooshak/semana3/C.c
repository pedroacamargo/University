#include <stdio.h>
#include <string.h> 

int main() {
	int N = 0;
	int K - 0;
	int linha[200];

	int index = 0;


	int maior = 0;

	if (scanf("%d", &N) != 1) return 1;

	for (int i = 0; i < N + 1; i++) { // linhas

		if (scanf("%d", &K) != 1) return 1;

		for (int j = 0; i < K; i++) {
			if (scanf("%d", &atual) != 1) return 1;

			if (atual > maior) {
				maior = j;
			} 
		}
	
		printf("%d",linha);		







	}	

	return 0;
}